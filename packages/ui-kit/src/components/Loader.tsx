import React from 'react';
import './Loader.scss';

const Loader = () => {
  return (
    <div className="Loader">
      <span className="Loader__spin"></span>
    </div>
  );
};

export default Loader;
