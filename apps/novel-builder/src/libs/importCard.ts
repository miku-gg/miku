import { MikuCard, TavernCardV2 } from "@mikugg/bot-utils";
import { NovelFormState } from "../state/NovelFormState";

export const tavernV2ToNovelState = (
  tavernV2: TavernCardV2
): NovelFormState => {};

export const mikuCardToNovelState = (
  mikuCardV2: MikuCard
): NovelFormState => {};
